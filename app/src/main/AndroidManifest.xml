<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mastermind">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/Theme.MasterMind">
        <activity android:name=".view_scores"></activity>
        <activity android:name=".show_users" />
        <activity android:name=".ViewWordsActivity" />
        <activity android:name=".AddWordsActivity" />
        <activity android:name=".AdminPannelActivity" />
        <activity android:name=".hard_level" />
        <activity android:name=".medium_level" />
        <activity android:name=".easy_level" />
        <activity android:name=".level_select" />
        <activity android:name=".Splash_Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".UserActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>